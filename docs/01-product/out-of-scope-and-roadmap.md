# Product — Out of Scope & Roadmap

> **Dependencies** (see `00-traceability.md`)
> - **Depends on:** `scope-v1.md`, the deferred items noted across `04-ai-engine/*`
> - **Consumed by:** `11-roadmap/*`
> - **Version:** v1

## Explicitly out of v1 (product boundary)
- **Exec/brand multi-subject monitoring** — the enterprise extension (maps to redact's exec-protection product).
- **Any on-platform actions** (deletion/editing) — that's redact's domain; we complement it.
- **Continuous monitoring / alerting** on new exposure — v1 is a point-in-time self-audit.

## Engine roadmap (deferred for cost/complexity, not capability gaps)
- **Adversary panel** (vs a single held-out adversary) for high-assurance safety claims — esp. at-risk **location**.
- VLM **crop/zoom** agentic tool; generative **inpaint** as a first-class image remediation.
- **Agentic Strategist loop** (AutoProfiler-style) for very large footprints.
- **Multimodal calibration** slice (fused text+image); per-language / per-platform calibration slices.
- **Judge self-consistency** / judge ensemble.
- Decoy as a fuller, consented **feature** beyond the opt-in.

## Product roadmap
Exec/enterprise multi-subject tier · continuous monitoring · **X paid-tier** live connector · on-device options.

Each deferral is a deliberate cost/scope call, not a missing capability — the v1 engine already specifies how these slot in.
