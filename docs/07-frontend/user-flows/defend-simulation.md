# Flow — Defend Simulation

> **Depends on:** `04-ai-engine/defend/*`, `06-api/endpoints/remediations.md`, `ethics-and-tone.md` · **Version:** v2 (entry via Attribution)

The "0.86 → 0.21" moment.

- **Entry:** from **Attribution** ("Break this inference") → a remediation run on `/defend/[code]`.
- See the **proven before → after** (calibrated, with **intervals** + the **value-recovery flip**), measured by the held-out adversary — not a self-report.
- Choose from the **privacy/utility frontier**: minimal generalization → stronger → remove → **[opt-in] decoy** (with its falsehood warning + **per-use confirm**). View the edit **diff** / the remediated image.
- **Advise-only:** "apply" = copy the rewrite / download the edited image; we **never** act on a platform.
- The **no-false-safety** note is persistent **and** shown prominently here (the riskiest moment for false comfort).
